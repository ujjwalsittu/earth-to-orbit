# Environment Variables Example (Refactored)
#
# This example removes repetition and clearly separates API (server)
# and Web (client) needs. Use placeholders here; configure real values
# in your hosting platform (Railway, Vercel, Netlify, etc.).
#
# API reads plain `process.env.*`. Web reads only `NEXT_PUBLIC_*`.
# Local dev: API can use project-root `.env`; Web can use `apps/web/.env.local`.

# =====================================================================
# Core Environment
# =====================================================================
NODE_ENV=development
ENV=development

# =====================================================================
# API (Server-side)
# =====================================================================
# Server
PORT=4000
FRONTEND_URL=http://localhost:3000

# Database
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/your-db?retryWrites=true&w=majority

# Auth tokens
JWT_SECRET=changeme-super-secret-jwt
JWT_REFRESH_SECRET=changeme-super-secret-refresh

# Email provider (Resend)
RESEND_API_KEY=changeme-resend-api-key

# Payments (Razorpay)
RAZORPAY_KEY_ID=changeme-razorpay-key-id
RAZORPAY_KEY_SECRET=changeme-razorpay-key-secret

# Storage (AWS S3)
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=changeme-aws-access-key-id
AWS_SECRET_ACCESS_KEY=changeme-aws-secret-access-key
AWS_S3_BUCKET=changeme-s3-bucket

# Company branding (used by API for emails, invoices, etc.)
COMPANY_NAME=Earth To Orbit
COMPANY_EMAIL=support@example.com
SUPPORT_EMAIL=support@example.com
COMPANY_WEBSITE=https://example.com
COMPANY_PHONE=+1-000-000-0000
COMPANY_ADDRESS=123 Example St, City, Country

# Demo/Seed data (API-only; used for local seeding)
DEMO_PLATFORM_ADMIN_EMAIL=admin@example.com
DEMO_PLATFORM_ADMIN_PASSWORD=changeme-admin-password
DEMO_ORGANIZATION_NAME=Example Org
DEMO_ORGANIZATION_EMAIL=org@example.com
DEMO_ORG_ADMIN_EMAIL=orgadmin@example.com
DEMO_ORG_ADMIN_PASSWORD=changeme-org-admin-password
DEMO_ORG_MEMBER_EMAIL=member@example.com
DEMO_ORG_MEMBER_PASSWORD=changeme-org-member-password

# =====================================================================
# Web (Client-side) — MUST be prefixed with NEXT_PUBLIC_
# =====================================================================
# Public environment flags
NEXT_PUBLIC_ENV=development

# URLs
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4000

# Payments (public key only)
NEXT_PUBLIC_RAZORPAY_KEY_ID=changeme-razorpay-key-id

# Company branding (shown in the UI)
NEXT_PUBLIC_COMPANY_NAME=Earth To Orbit
NEXT_PUBLIC_COMPANY_EMAIL=support@example.com
NEXT_PUBLIC_SUPPORT_EMAIL=support@example.com
NEXT_PUBLIC_COMPANY_WEBSITE=https://example.com
NEXT_PUBLIC_COMPANY_PHONE=+1-000-000-0000
NEXT_PUBLIC_COMPANY_ADDRESS=123 Example St, City, Country

# Feature flags
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true

# Attribution / Developer (optional)
NEXT_PUBLIC_DEVELOPER_NAME=Your Name
NEXT_PUBLIC_DEVELOPER_WEBSITE=https://your-website.example

# Demo credentials (UI previews — do not use in production)
NEXT_PUBLIC_DEMO_ADMIN_EMAIL=admin@example.com
NEXT_PUBLIC_DEMO_ORG_ADMIN_EMAIL=orgadmin@example.com
NEXT_PUBLIC_DEMO_ORG_MEMBER_EMAIL=member@example.com

# =====================================================================
# Deployment Notes
# =====================================================================
# - Production: Configure API secrets in your hosting dashboard. Do not rely
#   on filesystem `.env` in prod.
# - Web: Only expose `NEXT_PUBLIC_*` variables. These are embedded client-side.
# - Keep `FRONTEND_URL` (API) and `NEXT_PUBLIC_SITE_URL` (Web) aligned.
# - Keep `RAZORPAY_KEY_ID` (API secret counterpart) and
#   `NEXT_PUBLIC_RAZORPAY_KEY_ID` (Web public) consistent; never expose
#   `RAZORPAY_KEY_SECRET` publicly.